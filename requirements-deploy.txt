# Lightweight web/API deployment dependencies.
# Supported runtime: CPython 3.10-3.14, CPU only.
--extra-index-url https://download.pytorch.org/whl/cpu

# Linux and Windows use the explicit CPU build. PyTorch's macOS wheel is
# already CPU/Metal-only and does not carry the "+cpu" version suffix.
torch==2.10.0+cpu; sys_platform == "linux" or sys_platform == "win32"
torch==2.10.0; sys_platform == "darwin"

# Direct NLP dependencies used by web_server.py.
click==8.1.8
spacy==3.8.13
spacy-stanza==1.0.4
stanza==1.6.1
